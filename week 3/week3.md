## Competency Claim — Week 3

**C2 Code literacy and documentation**
I can read a buggy Python script, identify what each section does, and fix 
errors based on the error messages. Evidence: I diagnosed and fixed three bugs 
in week3_analysis_buggy.py, a ValueError from non-numeric data, a reversed 
sort logic returning lowest instead of highest scores, and empty role values 
being counted as valid data.

**C3 Data cleaning and file handling**
I can write a Python script that loads a messy CSV, normalizes inconsistent 
text formatting across multiple columns, and writes the cleaned output to a 
new file. Evidence: my script standardizes capitalization in the role, 
department, and primary_tool columns and outputs week3_survey_cleaned.csv.